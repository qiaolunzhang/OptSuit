for {(i,j) in Ep_u} {
    printf "(i,j) = %s, %s \n", i, j;
    for {path in 1..n_path['vn1_1', 'vn1_2']: (i,j) in path_link['vn1_1','vn1_2',path]} {
        printf "beta_log * q = %s * %s <= beta = %s \n", beta_log['vn1_1', 'vn1_2',0], q['vn1_1','vn1_2',path,0], beta[i,j,0];
    }
}
